## 📘 학습한 내용

- **탐색 순서 역발상**: 최대 크기부터 탐색 → 발견 시 즉시 종료로 실행 시간 단축
- **배열 초기화 오버헤드 제거**: `new boolean[]` 할당 → 전역 `int[]` 선언 후 `visitedId`를 증가시키는 방식으로 **O(1)** 최적화

## 📝 문제 요약

> 
> 
> - N x N 지역에서 대각선으로 움직이며 사각형 경로 탐색
> - **조건**: 출발점 복귀, 지역 이탈 불가, 같은 종류 디저트 중복 섭치 불가
> - **목표**: 조건 만족하는 최대 디저트 수 출력 (불가능 시 -1)

---

## 💡 1. 나의 접근 방식 & 핵심 아이디어

- **알고리즘/자료구조:** 브루트 포스 (크기 역순 탐색), 1/2차원 배열
- **시간/공간 복잡도:**
    - 시간: **O(N^4)** (가능한 모든 사각형 조합 탐색 → 단, 조기 종료 덕분에 실제 시간 훨씬 짧음)
    - 공간: **O(N^2)**(맵) + **O(1)**(크기가 101로 고정된 방문 체크 배열)
- **핵심 로직:**
    1. **크기 역순 탐색** → 조기 종료: 사각형 둘레 길이(`lenSum`) 최대치부터 작아지며 탐색
        
        → 조건 맞는 사각형 찾는 순간 무조건 최댓값 
        
    2. `visitedId` **방문 처리:** 전역 `int[] visited` 선언 후 매 탐색마다 `visitedId++` 처리
        
        → `visited[dessert] == visitedId`로 확인하여 **O(1)** 비용으로 갱신
        
    3. **검증 로직:** 가로세로 길이 `a`, `b`를 인자로 받아 `diag` 함수로 가능여부 검증

---

## ✨ 2. 나의 최종 코드

### 핵심 구현 코드

- **크기 역순 탐색 로직**
    
    ```jsx
    static void play() {
        int num = 0;
        // 사각형 둘레 길이(lenSum)를 최대부터 감소
        for (int lenSum = N + 1; lenSum >= 4; lenSum--) {
          num++;
          for (int i = lenSum - 2; i >= (lenSum + 1) / 2; i--) {
            int j = lenSum - i;
            for (int r = 0; r < num; r++) {
              for (int c = j - 1; c <= N - i; c++) {
                if (i != j) { // 직사각형
                  if (diag(c, r, i, j) || diag(N - c - 1, r, j, i)) {
                    ans = (lenSum - 2) * 2; // 찾으면 바로 종료
                    return;
                  }
                } else { // 정사각형
                  if (diag(c, r, i, i)) {
                    ans = (lenSum - 2) * 2;
                    return;
                  }
                }
              }
            }
          }
        }
      }
    ```
    
- `visitedId`를 활용해 디저트 중복 검사
    
    ```jsx
    static boolean diag(int x, int y, int a, int b) {
        visitedId++; // 매 탐색마다 고유 ID 부여 -> 배열 초기화 0초
        int nx = x;
        int ny = y;
        
        for (int idx = 0; idx < 4; idx++) {
          int limit = (idx % 2 == 0) ? a - 1 : b - 1;
          for (int i = 0; i < limit; i++) {
            nx += dx[idx];
            ny += dy[idx];
    
            int dessert = map[ny][nx];
    
            // 이번 탐색 사이클에서 이미 먹은 디저트 -> 실패
            if (visited[dessert] == visitedId)
              return false;
    
            // 디저트 먹음 처리 (현재 탐색 ID 마킹)
            visited[dessert] = visitedId;
          }
        }
        return true;
      }
    }
    ```
    

---

## 🤔 3. 문제 회고 (Retrospective)

### 🐾 3-1. 오류 해결 과정 (Troubleshooting Log)

- **개선필요**: 탐색 경로 검증 시 매번 isContained = new boolean[101] 선언 → 격자 크기 증가 시 힙 메모리 낭비
- **해결**: 전역 `int[] visited` 선언 후 탐색 시마다 `visitedId++` -> **O(N)**을 **O(1)**로 개선
- **리팩토링**: diag, square 로 분리되어 있던 메서드 병합

### **🌱 3-2. 새롭게 알게 된 점 (Learning Points)**

- `visitedId` 테크닉: 상태 초기화가 잦을 때, 정수형 고유 ID를 덧씌우는 방법

### 🧐 3-3. 더 궁금한 점 & 다음 목표 (Further Questions)

- `visitedId` 기법 활용해보기
