<aside>
## 📘 학습한 내용

- **역방향 시뮬레이션(Reverse Simulation)**: 치즈 ‘제거’ → 시간을 거꾸로 돌려 치즈 ‘추가’
- **비트마스킹(Bigmasking)**: 4방향 탐색 결과를 정수 하나에 압축 저장
- **Union by Size 최적화**: 그룹 병합 시 가장 큰 그룹으로 흡수 → 연산량 최소화
</aside>

## 📝 문제 요약

> 
> 
> - N x N 크기 치즈. 칸마다 1~100의 맛있는 정도 존대
> - 요정은 X일차에 맛있는 정도가 X인 칸을 먹어치움.
> - **목표**: 100일 동안 치즈 덩어리가 가장 많을 때의 최대 덩어리 수 구하기

---

## 💡 1. 나의 접근 방식 & 핵심 아이디어

- **알고리즘/자료구조:** 역순 시뮬레이션 / DFS / 비트마스킹 / 2차원,1차원 배열
- **시간/공간 복잡도:**
    - 시간: `O(D * N^2)` → 단, 큰 그룹 병합 최적화로 실연산 대폭 감소
    - 공간: `O(N^2)`
- **핵심 로직:**
    1. **시간 역행**: 요정이 다 먹은 날 (`maxDate`) → 1일차로 거꾸로 치즈 생성
    2. **그룹 탐색 및 병합 (4 방향 탐색):**
        - 인접 0개 → 새 그룹 ID 발급 (`groupId`)
        - 인접 1개 → 해당 그룹에 편입
        - 인접 2개 이상 → 인접 그룹 중 **가장 큰 그룹**으로 나머지 그룹 전체 흡수(DFS)
    3. **가지치기 (Pruning)**: 최대 가능 덩어리 개수(`N*N/2`) 도달 → 즉시 탐색 종료

---

## ✨ 2. 나의 최종 코드

### 핵심 구현 코드

- **역순 시뮬레이션 및 프루닝**
    
    ```jsx
    // 시뮬레이션 함수
        static void play(){
            // 날짜를 감소시키며 그루핑 (역순 시뮬레이션)
            for(int date = maxDate ; date > 0 ; date--){
                for(int i=0; i<N; i++){
                    for(int j=0; j<N; j++){
                        if(map[i][j] == date){ // 해당 날짜에 먹히는 치즈 생성
                            grouping(j, i);    
                        }
                    }
                }
                // 순회 후 ans 갱신
                if(ans < cnt){
                    ans = cnt;
                }
                if(ans == N*N/2) break; // 최대값(N*N/2) 도달하면 가지치기
            }
        }
    ```
    
- **비트마스크 탐색 및 Union by Size 최적화**
    
    ```jsx
    // 주변 그룹을 합병하는 함수
        static void groupMerge(int x, int y, int bitMask){
            int maxDir = -1;
            int maxSize = 0;
            // 주변 중 가장 큰 그룹 찾기 (연산량 최소화)
            for(int i=0; i<4; i++){
                if((bitMask & (1<<i))!=0){
                    if(maxSize < groupSize[groupMap[y+dy[i]][x+dx[i]]]){ 
                        maxSize = groupSize[groupMap[y+dy[i]][x+dx[i]]];
                        maxDir = i;
                    }
                }
            }
    
            int group = groupMap[y+dy[maxDir]][x+dx[maxDir]]; // 가장 큰 그룹 번호
            groupMap[y][x] = group;
            for(int i = 0; i < 4; i++){
                if(i == maxDir) continue;
                // 가장 큰 그룹과 다른 그룹이라면 병합 진행
                if((bitMask & (1<<i)) != 0 && groupMap[y+dy[i]][x+dx[i]] != group){
                    cnt--; // 덩어리 병합 -> 총 개수 1 감소
                    changeGroup(x+dx[i],y+dy[i], groupMap[y+dy[i]][x+dx[i]], group);
                }
            }
        }
    ```
    
- **DFS 그룹 ID 일괄 갱신**
    
    ```jsx
    // from그룹 좌표를 group그룹으로 합병 (DFS)
        static void changeGroup(int sx, int sy, int from, int group){
            groupMap[sy][sx] = group;
            groupSize[from]--;
            groupSize[group]++;
            
            for(int  i = 0; i<4; i++){
                int nx = sx + dx[i];
                int ny = sy + dy[i];
                // 인접 칸 중 기존 그룹(from)에 속한 칸을 따라가며 연쇄 갱신
                if(nx >= 0 && nx < N && ny >= 0 && ny < N && groupMap[ny][nx] == from){
                    changeGroup(nx, ny, from, group);
                }
            }
            return;
        }
    ```
    

---

## 🤔 3. 문제 회고 (Retrospective)

### 🐾 3-1. 오류 해결 과정 (Troubleshooting Log)

- **이슈1**: 오타(`groupMap[ny][ny]`) → 잘못된 메모리 참조 및 런타임 에러
- **이슈2**: `cnt` 변수 역할 혼용 (총 덩어리 수 & 새 그룹 ID) → 인덱스 초과, 중복 이름 그룹 생성
    
    ⇒ ID 발급용 변수 `groupId` 생성 → `cnt`와 분리
    

### **🌱 3-2. 새롭게 알게 된 점 (Learning Points)**

- **역방향 시뮬레이션**: “삭제하면서 분리” 보다 추가하며 “병합” 이 직관적이고 빠를 수 있음.

### 🧐 3-3. 더 궁금한 점 & 다음 목표 (Further Questions)

- BFS 구조나, Disjoint Set (Union-Find) 알고리즘으로 리팩토링